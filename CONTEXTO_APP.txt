MotoRide — registro de contexto / cambios recientes
Ultima actualizacion: 2026-05-01 (bloque 33 + historial sin duplicados / curvas / resumen)

=== Contacto y soporte ===
- Correo publico de contacto: motorideapp1@gmail.com
  - src/lib/clientInfo.ts: constante SUPPORT_EMAIL y mailto de soporte.
  - src/components/MapView.tsx: User-Agent cortesia Nominatim con el mismo correo.
- Cuenta admin (panel) y reglas Firestore siguen usando juarp123@gmail.com salvo que se migre a mano.

=== iPhone / Safari (textos de la app) ===
- src/App.tsx: modal de instalacion PWA — Safari (Compartir → Añadir a pantalla de inicio); Chrome iOS (CriOS: Compartir → Ver mas / mas acciones → Añadir a pantalla de inicio). Pantalla completa y mensajes alineados con ambos flujos.
- src/components/MapView.tsx: mensaje inclinometro iPhone simplificado.
- src/hooks/useVoiceChat.ts: permisos de microfono (Safari «aA», Ajustes) y aviso si no hay getUserMedia (abrir fuera de apps embebidas).

=== HUD del mapa (velocidad) ===
- Tiempo en ubicacion: Open-Meteo sin API key (temperatura actual + codigo WMO del dia para icono).
- src/hooks/useOpenMeteoWeather.ts, src/lib/weatherWmo.ts (Lucide).
- Bloque velocidad: mas margen izquierdo, HUD algo mas alto; icono + °C encima de km/h.

=== MirrorLink (ajustes / menu GPS en MapView) ===
- Estado unificado touchLockKind: 'pocket' | 'mirrorlink' | null.
- MirrorLink: pantalla completa, orientacion horizontal, misma cuenta atras 30 s que bolsillo.
- Tras 30 s: pantalla «Calibrando inclinacion» (~800 ms) con calibrate(), luego mapa visible con capa tactil bloqueada (anti toques fantasma).
- Desbloqueo: 3 toques + pulsacion larga (igual que bolsillo); candado discreto arriba a la izquierda en MirrorLink bloqueado.
- Modo bolsillo: sin cambio de flujo (bloqueo al acabar 30 s).

=== Mapa / tema ===
- Eliminado el interruptor «Modo oscuro / claro» del mapa: teselas siempre Carto Voyager (claro); sin estado isDarkMode en MapView.

=== Archivos de puntero ===
- contexto_app.txt remite a este archivo.

(contexto_app.txt -> ver este archivo)

=== RESUMEN COMPLETO DE CAMBIOS (PARA CONTINUIDAD EN ESCRITORIO) ===
Fecha: 2026-04-14

1) ESTADO GENERAL
- Se revisaron y corrigieron incidencias en invitaciones, inclinometro, planificador de rutas, reconexion de voz y UI en iPhone.
- La app compila correctamente en local con `npm run build`.
- Se dejo lista la configuracion de deploy con Wrangler para publicar assets de `dist`.

2) INVITACIONES DE AMISTAD / RUTA
- Archivo: `src/components/FriendsModal.tsx`
  - Unificacion de IDs para usar siempre el ID canonico de Firestore (`doc.id`) como `id` y `uid`.
- Archivo: `src/components/InviteFriendsModal.tsx`
  - Misma normalizacion de IDs al cargar amigos.
  - Fallback robusto al invitar:
    - Si da `permission-denied` por desincronizacion de amistad, intenta resincronizar `friends` y reintenta el envio.
    - Mensaje claro al usuario si no se puede completar.

3) MAPA / PLANIFICADOR / AVISOS
- Archivo: `src/components/MapView.tsx`
  - Rutas: se usa referencia de sugerencia seleccionada para evitar destinos equivocados por texto ambiguo.
  - Avisos: ajuste de stacking y alturas para que no se pisen ni corten la interfaz.
  - iPhone: se agrego modo de UI horizontal forzada cuando falla el lock nativo.
  - Mejoras de layout para que, con uno o varios avisos, el HUD no quede tapado.

4) INCLINOMETRO (SIMETRIA, ESTABILIDAD, FLUIDEZ)
- Archivo: `src/hooks/useLeanAngle.ts`
  - Fusion de roll de `deviceorientation` + `devicemotion` (gravedad) para reducir sesgo lateral.
  - `dynamicBiasRef` para autocentrado progresivo en parado y relajacion al moverse.
  - Ajuste de deadzones y umbrales para deteccion mas equilibrada.
  - A altas velocidades aumenta ligeramente sensibilidad.
  - Salida con precision decimal (mas fluida en pantalla).
  - `calibrate()` reinicia tambien el bias dinamico.
- Archivo: `src/components/MapView.tsx`
  - Eliminada ventana de maximos de 60 segundos.
  - Se muestran solo maximos globales: izquierda (azul) y derecha (rojo).
  - En modo tipo screen-share, parado y casi neutro, fuerza a 0 para evitar deriva espuria.
  - Autocalibracion tambien en uso normal GPS (sin bolsillo/mirrorlink), condicionada por estabilidad de rumbo/angulo.

5) CURVAS (CONTEO)
- Archivo: `src/components/MapView.tsx`
  - Minimo de velocidad para contar: 15 km/h.
  - Inicio de curva: inclinacion > 15º.
  - Fin de curva con umbral de salida mas bajo para evitar rebotes.
  - Puntuacion/registro condicionado por velocidad minima.

6) VOZ (RECONEXION AUTOMATICA)
- Archivo: `src/hooks/useVoiceChat.ts`
  - Se anade `joinedVoiceRoomRef` para recordar intencion de estar en sala.
  - `rebuildVoiceRoom` para reconstruir peers y reemitir join de forma segura.
  - Reconexion automatica en eventos de `connect/reconnect` de socket.
  - Limpieza controlada en `disconnect` manteniendo estado para recuperar.
  - Listener de `visibilitychange` para rearmar voz al volver a primer plano (especialmente iOS/Safari).

7) BUILD / ENTORNO
- Se ejecuto instalacion de dependencias (`npm ci`) al faltar herramientas en entorno.
- Se corrigieron errores TypeScript puntuales aparecidos durante refactor:
  - `Cannot find name 'y'` en `useLeanAngle.ts`.
  - `Cannot find name 'viewportSize'` en `MapView.tsx`.
- Build final verificado OK.

8) DEPLOY CLOUDFLARE (ULTIMO CAMBIO)
- Problema observado en CI/CD:
  - `npx wrangler versions upload` fallaba por:
    - `Missing entry-point to Worker script or to assets directory`
- Solucion aplicada:
  - Archivo nuevo: `wrangler.jsonc`
  - Contenido configurado para assets estaticos:
    - `"assets": { "directory": "./dist" }`
    - `compatibility_date` definida.
- Estado de verificacion:
  - El error de entry-point/assets queda resuelto.
  - En entorno no interactivo ahora exige `CLOUDFLARE_API_TOKEN` para desplegar.
  - Para que despliegue automatico funcione, definir variable de entorno `CLOUDFLARE_API_TOKEN` en CI/hosting.

9) GIT (ULTIMO TRAMO RELACIONADO CON DEPLOY)
- Rama: `cursor/fix-wrangler-assets-upload-eb2a`
- Commit deploy Wrangler:
  - `7d818b8` - Add wrangler config for dist assets upload
- Push realizado a remoto.

10) SIGUIENTE PASO RECOMENDADO EN ESCRITORIO
- Abrir este archivo (`CONTEXTO_APP.txt`) y continuar desde la rama actual.
- Verificar variable `CLOUDFLARE_API_TOKEN` en el proveedor de CI/deploy.
- Reintentar deploy con `npx wrangler versions upload` (o pipeline equivalente).

=== RESUMEN DE CAMBIOS DESDE EL ULTIMO LOG (2026-04-14, bloque 11) ===

11) GPS / BUSCAR Y CREAR RUTAS (PROVINCIA Y MUNICIPIO)
- Nuevo: `src/lib/reverseGeocode.ts`
  - `requestUserLocation()` (geolocation del navegador) y `reverseGeocodeProvinceMunicipality(lat, lon)` vía Nominatim reverse (es), User-Agent con contacto motorideapp1@gmail.com.
- Nuevo: `src/lib/nominatimPick.ts` — puntuación de resultados de búsqueda Nominatim para priorizar ciudad/municipio frente a POIs sueltos.
- `src/components/Dashboard.tsx`
  - Al cargar y al abrir el modal de crear ruta, relleno sugerido de provincia/municipio desde GPS; texto de ayuda en explorar y bajo los inputs del formulario de creación.
  - Historial de rutas retirado del dashboard (pasa al perfil).
  - Avatar del header con anillo de nivel/premium (`getLevelRingWrapperClass` desde `src/lib/levelRing.ts`).

12) PERFIL, MOTO E HISTORIAL DE RUTAS
- `src/components/Profile.tsx`
  - Campo de texto «Tu moto» (Firestore `motorcycle`, hasta 120 caracteres).
  - Sección colapsable «Historial de rutas» (consulta `rideHistory`), borrar entrada, «Repetir ruta».
- `src/components/MainApp.tsx` — `onRepeatRoute`: al repetir, cierra perfil y pasa la ruta al flujo principal (`repeatedRoute`).
- `firestore.rules` — validación `isValidOptionalString('motorcycle', 120)` en usuarios.
- Despliegue remoto de reglas ejecutado sobre el proyecto `motoapp-3e6c6` (`firebase deploy --only firestore:rules`).

13) NIVEL — «ARO» ALREDEDOR DEL JUGADOR
- Nuevo: `src/lib/levelRing.ts`
  - `getLevelRingBoxStyle(level, isPremium)` para marcadores HTML del mapa.
  - `getLevelRingWrapperClass(level, isPremium)` para componentes React (Tailwind): tramos por nivel (5, 8, 12, 18, 25, 35+) y estilo premium; algo de animación en niveles altos.
- `src/components/MapView.tsx` — avatares en mapa y lista de ranking envueltos con estos estilos según nivel/premium.

14) VISIBILIDAD DE RUTAS EN EL EXPLORADOR
- Nuevo: `src/lib/routeListing.ts` — tipos `public` | `friends_only` | `unlisted`, etiquetas y `canShowRouteInExplore` + `normalizeFriendIds`.
- `firestore.rules` — validación opcional de `routeListing` en documentos de rutas.
- `firestore.indexes.json` — ajustes de índices asociados a consultas.

15) INCLINACION / MOTOCICLETA (ESTIMACION AVANZADA)
- Nuevo: `src/hooks/useMotorcycleLean.ts` y carpeta `src/lib/motorcycleLean/` (filtros, geometría, detección bolsillo/curva, tipos).
- `src/hooks/useLeanAngle.ts` — integración/refactor acorde al nuevo enfoque donde aplica.

16) MENSAJES GLOBALES Y BUZON DE INVITACIONES
- Nuevo: `src/contexts/AppMessageContext.tsx` — modal reutilizable (info / éxito / error) para mensajes de app.
- `src/App.tsx` — envuelve la app con `AppMessageProvider`.
- Nuevo: `src/components/InvitesMailboxModal.tsx` y cambios en `InviteFriendsModal.tsx`, `GroupPanel.tsx`, `PendingRideInviteOverlay.tsx` para flujo de invitaciones y UX.
- `src/components/AdminPointsPanel.tsx` — ajustes puntuales de panel admin.

17) MAPVIEW Y OTROS
- `src/components/MapView.tsx` — cambios amplios (integración anillos de nivel, rutas/Nominatim, avisos y comportamiento ya descrito en bloques anteriores según diff).

18) GIT (ESTE BLOQUE)
- Objetivo: commit unificado en `main` con el listado anterior y push a `origin/main`.

=== RESUMEN DESDE EL ULTIMO LOG (POST BLOQUE 18) ===

19) NOVEDADES (MODAL), PRECIPITACIONES Y TEXTO DE CONTEXTO
- `src/components/WhatsNewModal.tsx`
  - Version `WHATS_NEW_VERSION` actualizada para volver a mostrar el modal una vez por dispositivo.
  - Cabecera: logo de la app (`ICONO.png` en raiz, misma fuente que Login) en lugar del icono de estrella; reflejo bajo el logo (imagen invertida + mascara + pulso suave) y franja de brillo animada (`whats-new-logo-gleam` / `whats-new-reflection-pulse` en `src/index.css`).
  - Lista de cambios reescrita desde cero: un solo dia (14 abr 2026) con entradas que cubren bloques 11-18 de este archivo mas aviso de lluvia en mapa, toque en nivel al perfil, bandeja de invitaciones centrada en movil, sincronizacion puntos/nivel, scroll planificador, invitar en proximas rutas, reorden de «Rutas de amigos», lista de apuntados, visibilidad de rutas, GPS/Nominatim, perfil/moto/historial, aros de nivel, explorador, mensajes globales/buzon, inclinacion/invitaciones amistad, ajustes de mapa/HUD.
- `src/lib/precipitationRisk.ts` + `src/components/MapView.tsx` (ya en main desde commit anterior)
  - Consulta Open-Meteo en posicion, anillo ~10 km y puntos de la ruta; banner ~30 s, cooldown entre avisos.
- Deploy Cloudflare Workers: nombre en `wrangler.jsonc` puede diferir del Worker enlazado en CI (p. ej. esperado `app`); el build puede sobreescribir el nombre; alinear nombre en dashboard o en config si se quiere evitar el aviso.

=== RESUMEN DESDE EL ULTIMO LOG (POST BLOQUE 19) ===

20) CTA INICIO, TUTORIAL INSTALACION CHROME IOS, NOVEDADES SIMPLIFICADAS
- `src/components/Dashboard.tsx`
  - Bloque «¿Listo para rodar?»: deja el bloque naranja plano; ahora fondo zinc con gradiente, borde y halo naranja, logo pequeño en esquina, texto en zinc-400. Botones: «Crear ruta» con gradiente naranja/ámbar; «Unirse con código» estilo zinc; columna en móvil y fila desde `sm`; chevron decorativo solo en `sm+`.
- `src/App.tsx`
  - Estado `isChromeIOS` (`CriOS` + iPhone/iPad/iPod).
  - Panel «Cómo ponerla en tu móvil» visible si `beforeinstallprompt`, Safari, Chrome iOS o iOS en general.
  - Ramas: Android/escritorio con `beforeinstallprompt`; Chrome iPhone (Compartir → Ver más → Añadir a pantalla de inicio); Safari iOS (lista corta); otros iOS (texto genérico); escritorio sin iOS (instalación desde navegador).
  - Bloque pantalla completa también si `isChromeIOS`. Mensajes de error/info de pantalla completa actualizados (Safari y Chrome iOS).
- `src/components/WhatsNewModal.tsx`
  - `WHATS_NEW_VERSION` `2026.04.16`. Lista reducida a frases simples (`HIGHLIGHTS`), sin horas ni etiquetas novedad/arreglo; botón «Entendido».

=== RESUMEN DESDE EL ULTIMO LOG (POST BLOQUE 20) ===

21) PLANIFICADOR DE RUTAS (CREAR RUTA) — DESTINOS DESDE CERO
- Nuevo: `src/lib/routePlannerDestination.ts`
  - Unifica URL y parametros de busqueda Nominatim (`countrycodes=es`, `addressdetails`, limite coherente con la lista).
  - Cabeceras `Accept-Language` y `User-Agent` con correo de contacto (politica de uso de Nominatim), alineado con `clientInfo` / MapView.
  - `fetchNominatimSuggestions`: orden con `sortNominatimResults` / `pickBestNominatimResult` desde `nominatimPick.ts` (misma prioridad ciudad/municipio que al generar).
  - `resolveDestinationForRouting`: prioridad (1) sugerencia elegida si el texto sigue coincidiendo (`destinationLabelsMatch` con normalizacion Unicode), (2) par lat/lon escrito (`tryParseCoordinatePair` con bbox Espana peninsular + islas), (3) geocodificacion con el mismo criterio de mejor resultado.
  - `formatOsrmDestCoords` para cadena `lon,lat` hacia OSRM.
- `src/components/Dashboard.tsx`
  - Busqueda con debounce ~600 ms y `AbortController`: al cambiar el texto se cancela la peticion anterior (menos carreras y resultados desfasados).
  - Ref `PickedDestination` con campo `label` (antes `displayName`); al editar el input se invalida la seleccion solo si ya no coincide el texto normalizado.
  - `generateLocalRoute` usa solo `resolveDestinationForRouting` + GPS en paralelo; mensajes distinguiendo red vs destino no encontrado.
- `src/components/WhatsNewModal.tsx`
  - Version actualizada; entrada en tablon sobre el planificador / busqueda de destinos (sin admin ni premium).

=== RESUMEN DESDE EL ULTIMO LOG (POST BLOQUE 21) ===

22) INCLINOMETRO (FUSION KALMAN, CINEMATICA, CERO DUAL) Y NOVEDADES
- Nuevo: `src/lib/motorcycleLean/kinematicLean.ts`
  - Guiñada efectiva ~ proyeccion de `rotationRate` (beta, gamma, alpha) sobre el vector de gravedad normalizado.
  - Roll cinemático en curva plana: atan2(v·ω, g) para no fiarse solo de la gravedad aparente (aceleración lateral en curva).
  - Peso de mezcla `kinematicBlendWeight` según |v| y |ω|.
- Nuevo: `src/lib/motorcycleLean/kalmanRoll1D.ts`
  - Filtro Kalman escalar en rad: predicción con ω_roll (eje de avance calibrado), correcciones secuenciales por medida acelerómetro (marco moto) y por medida cinemática.
- `src/hooks/useLeanAngle.ts` (reescritura del pipeline)
  - Solo `devicemotion` (sin mezcla primaria con `deviceorientation`): base moto agnóstica con `rotationFromTo` + `buildBikeBasisAtCalibration` + `leanDegFromGravityInBikeFrame` + `rollRateFromGyro`.
  - R de medición y ruido de proceso adaptados a bolsillo (`PocketInstabilityTracker`) y a plausibilidad de curva (`evaluateCurvePlausibility` / aceleración lateral).
  - Salida con low-pass extra en perfil bolsillo; offsets en `localStorage` separados manillar vs bolsillo (`STORAGE_HANDLEBAR` / `STORAGE_POCKET`) + migración desde clave legacy.
- `src/components/MapView.tsx` (ya integrado en sesiones anteriores)
  - `leanCalibrationProfile`: `pocket` mientras `touchLockKind !== null`; `handlebar` en GPS normal.
  - Auto-calibración por rumbo en recta no aplica con `touchLockKind` activo.
  - Tras bloqueo táctil (~850 ms): `calibrate()` para fijar cero IMU en bolsillo/MirrorLink.
- `src/components/WhatsNewModal.tsx`
  - `WHATS_NEW_VERSION` incrementada; lista `HIGHLIGHTS` actualizada con inclinometro y calibración dual (lenguaje claro para usuario).

=== RESUMEN DESDE EL ULTIMO LOG (POST BLOQUE 22) ===

23) INCLINOMETRO HIBRIDO (COMPLEMENTARIO + CINEMATICA) Y MAPA (ZOOM / BUFFER)
- `src/hooks/useLeanAngle.ts`
  - Por debajo de ~5 km/h: angulo desde gravedad en marco moto; sin mezclar termino cinemático v·ω/g.
  - Desde ~5 km/h: θ = atan((v·ω)/g) mezclado con acelerometro según peso; fusión con `complementaryAngleRad` (giro + corrección) en lugar del Kalman escalar.
  - Zona muerta reducida a baja velocidad para no forzar 0° si el dispositivo sigue inclinado.
- `src/lib/motorcycleLean/kinematicLean.ts`
  - `DEFAULT_KINEMATIC_MIN_SPEED_MPS` (5 km/h); ajuste de `kinematicBlendWeight`.
- `src/components/MapView.tsx` (`MapController`, `TileLayer`)
  - Zoom dinámico al grabar: interpolación lineal ~5 km/h → zoom 18, ~120 km/h → zoom 12, con suavizado; velocidad sin redondear para el seguimiento.
  - `keepBuffer` mayor en mapa base y capa radar cuando el mapa gira con el rumbo (menos “cuadrados” vacíos).
- Rama/merge: `cursor/hybrid-lean-map-zoom-6707` → `main`.

24) RESUMEN COMPARTIDO, GPX, PLANIFICADOR DE DESTINO (PRECISION)
- `src/lib/gpx.ts`
  - Parseo robusto: `trkpt`/`rtept` si no hay LineString; unión de segmentos / MultiLineString; validación XML.
- `src/components/MapView.tsx`
  - Imagen de resumen compartido rediseñada (vertical, tarjeta, logo); texto de compartir más completo; modal de fin de ruta con estética mejorada.
  - Subida GPX: mensaje de éxito, reset del input file, errores más claros.
- `src/components/Dashboard.tsx`
  - Misma lógica GPX/feedback al crear ruta; texto de ayuda: elegir sugerencia de la lista o coordenadas para destino fiable.
- `src/lib/routePlannerDestination.ts`, `src/lib/nominatimPick.ts`
  - `PickedDestination` con `osmType`/`osmId`; `fetchNominatimPlaceByOsmId` (lookup Nominatim) antes de OSRM para coords canónicas del lugar elegido.
  - Búsqueda Nominatim con `dedupe=1`.
- Rama/merge: `cursor/summary-gpx-planner-6707` → `main`.

=== AJUSTE POST-DEPLOY (GPX EN DASHBOARD) ===
- Problema inicial: en «Espontánea» la subida de GPX no mostraba UI (solo toast).
- Primera corrección: bloque GPX visible también en espontánea (luego retirado de espontánea por UX).

=== BLOQUE 25 — PLANIFICAR: GPX O BUSQUEDA (UX) ===
- `src/components/Dashboard.tsx`
  - **Espontánea:** sin bloque de subida GPX en el modal (el track se puede cargar desde la lupa / mapa GPS).
  - **Planificar:** zona «Archivo .gpx» arriba; al cargar un GPX se oculta el buscador Nominatim y solo se muestra tarjeta de ruta (distancia, tiempo estimado).
  - Tiempo estimado GPX: minutos = distancia / **45 km/h** (media orientativa en carretera; el .gpx suele no traer duración).
  - Botón **«Usar búsqueda»** quita el GPX y restaura el buscador (`gpxFromFile` + `clearPlannerSearchState`).
  - Ruta generada por **OSRM** (`generateLocalRoute`) pone `gpxFromFile = false` para no ocultar el buscador.
  - `generateLocalRoute` bloqueado si sigue activo GPX de archivo (mensaje informativo).
  - Al cerrar el modal de crear ruta se resetean GPX y destino.
  - Fallback provincia/municipio por primer punto del GPX al crear grupo (si sigue faltando) se mantiene.
- `src/components/WhatsNewModal.tsx`: `WHATS_NEW_VERSION` `2026.04.15b`; entrada sobre planificar con GPX vs buscador.

=== BLOQUE 26 — INCLINOMETRO: SIGNO IZQUIERDA / DERECHA ===
- `src/hooks/useLeanAngle.ts`
  - Constante `LEAN_SENSOR_SIGN = -1`: invierte medida de gravedad, roll rate del giro y término cinemático para que coincida con la UI (positivo = derecha, negativo = izquierda).
- `src/lib/motorcycleLean/geometry.ts`
  - Comentario de convención de signo: el alineado final lo hace `useLeanAngle`.

=== BLOQUE 27 — ICONOS DE GIRO EN NAVEGACION (GPS) ===
- Nuevo: `src/components/NavManeuverIcons.tsx`
  - Flechas de maniobra estilo plano / trazo grueso (giro en L con esquina redondeada, recto, leve, cerrado, cambio de sentido).
  - Rotonda: arco circular + flecha; salida de rotonda: variante propia.
  - Enlace / rampa (`on ramp` / `off ramp`): vía + rampa divergente; bifurcación e incorporación (`fork`, `merge`).
- `src/components/MapView.tsx`
  - Sustituye Lucide (`ArrowUp`/`RotateCw`) por estos SVG; tamaño ~32px compacto / 40px normal en el panel de instrucción.

=== BLOQUE 28 — INCLINOMETRO EN APaisado (HORIZONTAL) ===
- Problema: `DeviceMotion` usa ejes del hardware; al girar el teléfono a horizontal los ejes ya no coinciden con el algoritmo calibrado en vertical.
- Nuevo: `src/lib/motorcycleLean/deviceScreenFrame.ts`
  - `remapDeviceVectorToNaturalPortrait`: reexpresa aceleración y giro como si el dispositivo siguiera en orientación natural (0°/90°/180°/270° según `screen.orientation.angle`).
- `src/hooks/useLeanAngle.ts`
  - Gravedad, aceleración lineal y `rotationRate` (incl. `x`/`y`/`z` si existen) se remapean antes del low-pass y de la base moto.
  - Al cambiar el **bucket** de orientación (0/90/180/270): invalidar calibración, reiniciar buffer de gravedad y pocket tracker para evitar ángulos incoherentes.

=== AJUSTE BLOQUE 28 (SEGUNDA PASADA) ===
- `getScreenOrientationAngleDeg`: si `angle` es 0 pero `type` es landscape, o si el viewport es claramente apaisado (`innerWidth > innerHeight`), usar 90° para el remapeo (Safari a veces no actualiza `angle`).
- `getRemappedRotationRateDeg`: WebKit usa **Euler** (beta, gamma, alpha) en `rotationRate`, no mezclar con x,y,z cartesianos; remapear solo (β,γ,α) o solo (x,y,z).
- Inicialización del low-pass de gravedad: primera muestra en **marco remapeado** (`gLoc`), no en crudo del sensor (bug que rompía el filtro al girar).

=== BLOQUE 29 — PAUSA DE RUTA Y GASTOS (COMIDA / BEBIDA) ===
- `src/components/MapView.tsx`
  - **Pausa sin cerrar grupo:** anfitrión: botones Pausa / Reanudar junto a Finalizar; estado en Firestore `ridePaused`, `ridePauseStartedAt`, acumulado `pausedTimeMs` al reanudar y al finalizar.
  - **Tiempo de ruta:** al cerrar la grabación, la duración del resumen resta el tiempo en pausa (`pausedTimeMs` + tramo actual si sigue pausado).
  - **Sesión cerrada en pausa:** `sessionStorage` marca pausa; al volver a abrir la app con la ruta aún pausada, modal «Ruta en pausa» → Continuar.
  - **Gastos:** solo anfitrión; campo opcional en el resumen (€); reparto a escote según `members.length`; se guarda en `rideHistory` como `foodExpenseEuros`, `splitPerPersonEuros`, `memberCountForSplit` solo si hay importe > 0.
  - **Privacidad al compartir:** la imagen generada (`createSummaryImage`) no incluye gastos (solo métricas de conducción).
- `firestore.rules`: validación de campos opcionales en grupos (`ridePaused`, `ridePauseStartedAt`, `pausedTimeMs`) y en `rideHistory` (gastos).
- `src/components/WhatsNewModal.tsx`: versión de novedades actualizada; entrada breve sobre pausa y gastos.

=== AJUSTE HUD GPS — ICONOS Y ROTONDAS ===
- `src/components/NavManeuverIcons.tsx`: giros con trazo + punta (sin superposición fill/stroke que pixelaba); `shapeRendering="geometricPrecision"`.
- Rotonda: círculo + arco de sentido + **número de salida** (OSRM `exit`); salida de rotonda con flecha clara.
- `src/hooks/useNavigation.ts`: estado `roundaboutExit` desde el siguiente paso OSRM.
- `src/components/MapView.tsx`: contenedor del icono con gradiente y sin rotación que escalonaba bordes.

=== CORRECCION — UBICACION NO VISIBLE FUERA DE GRABACION ===
- Problema: `useLocationTracking` solo estaba activo con `isRecording && !ridePaused`, así que al abrir el mapa sin haber iniciado ruta (o en pausa) no había fix GPS y el marcador no aparecía.
- Cambio: tracking activo con grupo válido (`groupId` y no `REPEATED`). Inclinómetro y métricas de ruta siguen ligados a `rideActive`.

=== BLOQUE 30 — HUD EN RUTA: BOTONES PAUSA / FINALIZAR (ANFITRION) ===
- `src/components/MapView.tsx`
  - El contenedor del HUD inferior tenía `max-w-full overflow-hidden`: al subir km/h el bloque de velocidad ensanchaba la fila y **recortaba** la columna derecha (Pausa / Finalizar).
  - Ajuste: `overflow-visible`, `max-w-[min(100%,calc(100vw-1rem))]`, columna de velocidad con **ancho fijo** (`w-[5rem]` / `sm:w-[6.25rem]`), cifra `min-w-[3ch] text-center`, columna de puntuación + acciones con `shrink-0`.
  - Los botones Pausa y Finalizar del anfitrión pasan a mostrarse **en cuanto** `isRecording` (sin el umbral previo `localDistance >= 0.05` ~50 m), para que siempre estén disponibles al iniciar la grabación.

=== BLOQUE 31 — PARTICIPANTE: SALIR DE RUTA CON RESUMEN Y PUNTOS ===
- `src/components/MapView.tsx`
  - Antes: al salir un usuario no anfitrión solo se le quitaba del grupo; no se disparaba el efecto de fin de grabación (el host sigue con `isRecording: true`), así que **no** veía resumen ni se consolidaban puntos como al finalizar.
  - Ahora: si no eres anfitrión y sales con la ruta **en marcha** (`isRecording` y `startTime`), tras `deleteGroupIfHost` se calculan las mismas estadísticas que al parar la grabación (`computeCurrentSessionRideStats`: bonus distancia, multiplicadores `getActivePointsConfig`, tiempo descontando pausas), se llama a `commitRidePointsToProfile`, se guarda borrador (`persistRideDraft`) y se muestra el **mismo modal de resumen**.
  - Se reutiliza `rideSessionKey` / `ridePointsCommittedSessionKeyRef` para no duplicar puntos si el host sigue rodando después.
  - La navegación de salida (`prepareHistoryLeave`, `onLeave`, `history.back`) se **aplaza** hasta cerrar el resumen (botones Guardar / No guardar en historial) vía `pendingLeaveAfterSummaryRef` y `completeParticipantLeaveIfNeeded`.
- `src/components/WhatsNewModal.tsx`: versión de novedades actualizada; entradas ordenadas por orden de implementación en el producto (más antiguas arriba, más recientes abajo).

=== BLOQUE 32 — MAPA (POSICION SUAVE), HISTORIAL (TARJETAS), VOZ (RECONEXION UI) ===
- `src/components/MapView.tsx`
  - Posición mostrada interpolada hacia el GPS / snap a ruta (`smoothMapLocation`, refs `smoothMapTargetRef` / `smoothMapRef`, constantes `SELF_MAP_SMOOTH_ALPHA`, `SELF_MAP_SNAP_M`) para que la flecha del usuario y el seguimiento del mapa no vayan a saltos.
  - `MapController`: prop `smoothFollow` para relajar el anti-jitter (`tooSoon`) cuando se usa la posición suavizada y el centro no se queda atrás.
  - Componente `SmoothedPeerMarker`: actualización imperativa `setLatLng` con `requestAnimationFrame` y lerp hacia la posición objetivo de otros moteros (`PEER_SMOOTH_ALPHA`, `PEER_SNAP_M`); evita re-renders por frame.
- `src/lib/rideHistoryDisplay.ts` (nuevo)
  - `formatRideCardTitle`, `formatRideCardSubtitle` (es-ES), `rideHistoryPointsEarned` para listados de historial.
- `src/components/FriendsModal.tsx`
  - Historial de rutas del amigo: tarjeta vertical alineada con diseño de referencia (título naranja con nombre de ruta + fecha, línea fecha/hora, fila DIST. / PTS con puntos en `text-orange-400`); cabecera del perfil amigo con puntos totales en naranja.
- `src/components/Profile.tsx`
  - Misma estructura de tarjeta en «Historial de rutas»; barra de nivel: puntos actuales del usuario en naranja.
- `src/hooks/useVoiceChat.ts`
  - Estados `voiceTransportLost` (socket desconectado con voz activa) y `voiceMeshResetting` (~1,4 s al llamar `rebuildVoiceRoom`: reconexión socket, `connect`, `visibilitychange`).
  - Valor derivado `voiceReconnecting` expuesto al UI.
- `src/components/MapView.tsx` (botón micrófono)
  - Si `voiceReconnecting`: botón ámbar, icono `Loader2` animado, texto «Reconectando» debajo; badge de número de peers oculto durante la reconexión.
- `src/components/WhatsNewModal.tsx`
  - `WHATS_NEW_VERSION` `2026.04.30`: entradas para chat de voz (reconectando), historial en tarjetas con puntos en color, y geolocalización suave en mapa (lista completa en el modal).

=== BLOQUE 33 — VOZ (RECONEXIÓN FIABLE) Y VIENTO EN EL HUD ===
- `src/hooks/useVoiceChat.ts`
  - Handler unificado `onSocketTransportRestored`: primero limpia `voiceTransportLost` si hay voz activa y sala unida; luego `rebuildVoiceRoom` solo con stream y permisos (evita quedar en ámbar al volver la red).
  - Registrado en `socket.on('connect')` y `socket.io.on('reconnect')` (Manager); eliminado el antiguo `socket.on('reconnect')` del socket aislado.
  - `window.addEventListener('online')` como refuerzo si el socket ya está `connected`.
  - UI de malla WebRTC acortada (~900 ms); fail-safe ~4,5 s para limpiar `voiceMeshResetting` si el temporizador no dispara.
- `src/components/MapView.tsx` (micrófono)
  - Modo reconectando: tarjeta con borde/blur, botón oscuro con anillo ámbar, `Loader2`; texto «Reconectando» + «Restaurando enlace de voz»; voz activa estable en verde esmeralda (`emerald-500`).
- `src/hooks/useOpenMeteoWeather.ts`
  - Petición ampliada: `wind_speed_10m`, `wind_direction_10m`, `windspeed_unit=kmh`.
  - `windBlowToDeg`: dirección hacia la que sopla el viento (opuesta a la procedencia de la API); `windSpeedKmh`.
- `src/components/MapView.tsx` (HUD izquierdo)
  - Encima de temperatura/icono: fila con flecha (`ArrowUp` rotada según `windBlowToDeg`) y velocidad en km/h; mismo criterio Premium que el HUD meteorológico (`showHudWeather`).
- `src/components/WhatsNewModal.tsx`
  - `WHATS_NEW_VERSION` `2026.05.01`: entradas para viento en mapa y refuerzo de reconexión de voz (ver changelog).

=== BLOQUE 34 — HISTORIAL SIN DUPLICADOS, CURVAS 15°, RESUMEN EN PERFIL ===
- **Curvas:** cuenta solo si velocidad **> 20 km/h** (no ≥) e inclinación **> 15°**; salida de curva con umbral ~8° (`MapView`).
- **Historial:** `setDoc` id `${uid}_${groupId}_${startTime}` + `merge` (evita duplicados al pulsar Continuar varias veces); `durationMs`, `rideSessionKey` opcional; `routeGeoJSON` del grupo si existe.
- **Firestore:** `rideHistory` permite `update` al dueño para merges; validación de campos opcionales.
- **Perfil:** tocar tarjeta abre modal de resumen (curvas, tiempo, etc.); tarjetas con `formatRideCardTitle` / `rideHistoryPointsEarned` como en main.
